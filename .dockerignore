# The image carries the renderer, not anyone's card.
*
!build.js
!serve.js
!template.html
# ...but it does carry the demo card, shown until a real data/ is mounted.
!example.json
# package.json and the lockfile, for the build's own dependencies.
!package.json
!pnpm-lock.yaml
